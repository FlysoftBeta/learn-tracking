# 每日记录

文件按 `YYYY/MM/YYYY-MM-DD.md` 放置，仅在有内容的日子创建。使用用户指定的事件日期；补记时同时写明记录日期。不自动生成空白日记或催促复盘。

每天用短段落记录进展、当时的理解和决定。需要时增加“任务关联”“待整理”；任务使用 [待办](../TASKS.md) 的 ID 和链接，不复制可勾选清单。历史页的完成事件属于历史事实，当前状态仍以任务正文为准。

检索：按日期查文件，或用 `rg -n '关键词' journal`。有可独立复用的想法时物化到 notes，并在当天页面保留链接。
